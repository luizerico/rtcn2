# Dependencies
/node_modules
client/node_modules
client/.next
.env
build/
*.log*

# Local sample dumps and template schemas (not shipped with the app)
/01_sample_data/
/02_template_model/

# Local file storage (FILE_STORAGE_DRIVER=tmp)
/tmp/

# Local agent / git worktree scratch (not part of the app)
.worktrees/
.worktrees_trash/

# Python reports service
reports/.venv/
reports/**/__pycache__/
reports/**/*.py[cod]
reports/.pytest_cache/